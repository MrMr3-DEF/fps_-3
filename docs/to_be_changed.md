# Follow-up validation

The September audit fixes are implemented; see [audit resolution](audit-resolution.md).

- Before production rollout, exercise a real host plus two browser clients with Turnstile and a TURN-dependent network, including a new join after five minutes and recovery after a network change.
- Complete manual pointer-lock gameplay testing in a supported desktop browser. The embedded browser used during implementation rendered the arena but rejected pointer lock.

No additional feature requests are currently recorded.
